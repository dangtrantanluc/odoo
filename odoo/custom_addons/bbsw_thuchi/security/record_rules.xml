<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- ══ hr.attendance ══
         Nhân viên thường chỉ đọc/tạo bản ghi chấm công của chính mình.
         Admin (group_system) xem tất cả (rules không áp dụng cho superuser). -->
    <record id="rule_hr_attendance_own" model="ir.rule">
        <field name="name">Chấm công: nhân viên chỉ xem của mình</field>
        <field name="model_id" ref="hr_attendance.model_hr_attendance"/>
        <field name="groups" eval="[(4, ref('base.group_user'))]"/>
        <field name="domain_force">[('employee_id.user_id', '=', user.id)]</field>
        <field name="perm_read"  eval="True"/>
        <field name="perm_write" eval="True"/>
        <field name="perm_create" eval="True"/>
        <field name="perm_unlink" eval="False"/>
    </record>

    <record id="rule_hr_attendance_admin" model="ir.rule">
        <field name="name">Chấm công: admin xem tất cả</field>
        <field name="model_id" ref="hr_attendance.model_hr_attendance"/>
        <field name="groups" eval="[(4, ref('base.group_system'))]"/>
        <field name="domain_force">[(1, '=', 1)]</field>
        <field name="perm_read"   eval="True"/>
        <field name="perm_write"  eval="True"/>
        <field name="perm_create" eval="True"/>
        <field name="perm_unlink" eval="True"/>
    </record>

</odoo>
